is a tool for storing memory address of variable for direct access and modification and can reassign the value.

Say for example, it would be easier to point at the variable rather than moving it around so it's cost efficient solution mostly.