- it literally just tries to predict the next word
- the parameter count is a slider count meaning as the model trains it gets fixed in position
	- normally each parameter takes up to 16 decimal place accuracy meaning 7B model could take up to 14GB of VRAM
	