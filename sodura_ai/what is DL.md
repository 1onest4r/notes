is a further progression of unsupervised learning which that uses neural network layers to simulate how human brain works in hopes of achieving what human achieves

"Forward propagation" multiple layers of interconnected nodes progressively ingest data, recognize patterns and identify relationships to refine decisions

"Backward propagation" to calculate errors to change its weight and adjust system biases accordingly.

Most advanced AI applications such is LLMs like chatbots that uses deep learning require tremendous amount of computation power and resources.